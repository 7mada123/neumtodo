import 'package:flutter/material.dart';

const borderRadius5 = BorderRadius.all(Radius.circular(5));
const borderRadius10 = BorderRadius.all(Radius.circular(10));
const borderRadius15 = BorderRadius.all(Radius.circular(15));

const shapeBorderRadius10 = RoundedRectangleBorder(
  borderRadius: borderRadius10,
);

const paddingH10 = EdgeInsets.symmetric(horizontal: 10);
const paddingH15 = EdgeInsets.symmetric(horizontal: 15);
const paddingH20 = EdgeInsets.symmetric(horizontal: 20);
const paddingV20 = EdgeInsets.symmetric(vertical: 20);
const paddingV10 = EdgeInsets.symmetric(vertical: 10);

const paddingH20V20 = EdgeInsets.symmetric(
  horizontal: 20,
  vertical: 20,
);

const paddingH20V10 = EdgeInsets.symmetric(
  vertical: 10,
  horizontal: 20,
);

const twoYears = Duration(days: 720);
